{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/bin/debug/bindings",
      "args": [
        "main.b",
        "-log=none",
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}