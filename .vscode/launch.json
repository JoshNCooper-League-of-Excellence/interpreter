{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/bin/compiler",
      "args": [
        "tests/empty-main.q"
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}