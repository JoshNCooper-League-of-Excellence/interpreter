{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/bin/compiler",
      "args": [
        "tests/goto.q"
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}