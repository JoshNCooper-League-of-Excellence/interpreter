{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/bin/debug/bindings",
      "args": [
        "main.b",
        "-o=main.ir"
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}