{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/bin/compiler",
      "args": [
        "main.q", "-o=main.qir"
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}