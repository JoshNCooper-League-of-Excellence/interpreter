{
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Launch",
      "program": "${workspaceFolder}/bin/debug/bindings",
      "args": [
        "tests/expr_bin.b",
      ],
      "cwd": "${workspaceFolder}"
    }
  ]
}